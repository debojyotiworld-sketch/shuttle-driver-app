<resources>

    <!-- Base application theme. -->
    <style name="Theme.DriverApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.DriverApp</item>
    </style>

</resources>
